name: Build and Deploy
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2 

      - name: Install and Build 🔧 
        run: |
          pnpm install
          pnpm run build

      - name: Deploy 🚀
        uses: shuguangnet/github-pages-deploy-action@releases/v3
        with:
          ACCESS_TOKEN: Qbcjfj04828945WQJfxvfhfghf
          BRANCH: gh-pages
          FOLDER: build
